import { Numeric, Locale } from "./numeric";

export const sum = Numeric.sum;
export const subtract = Numeric.subtract;
export const multiply = Numeric.multiply;
export const divide = Numeric.divide;

export const min = Numeric.min;
export const max = Numeric.max;
export const clamp = Numeric.clamp;

export const gt = Numeric.gt;
export const gte = Numeric.gte;
export const lt = Numeric.lt;
export const lte = Numeric.lte;
export const eq = Numeric.eq;

export const round = Numeric.round;
export const ceil = Numeric.ceil;
export const floor = Numeric.floor;
export const abs = Numeric.abs;

export const average = Numeric.average;
export const percent = Numeric.percent;

export const parse = Numeric.parse;

export { Locale };
export { Numeric };
